# Optional: canonical + Open Graph / Twitter image base URL (no trailing slash).
# On Vercel Production with custom domain vacationvip.com, set this in the project
# Environment Variables so built HTML does not use *.vercel.app in meta tags.
# VITE_SITE_BASE=https://vacationvip.com/getaways/vvip-uvc-hyatt-riviera-cancun-299
